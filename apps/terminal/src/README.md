# @velt/terminal — STUB

Not implemented. The React terminal UI lands after the daemon contract settles.

Doctrine §8 makes terminal-grade polish a product requirement: density,
alignment, monospace discipline, keyboard-first interaction, zero layout jitter.
Nothing here yet claims to satisfy that.

Consume the daemon through `@velt/api-client`, never with a hand-written fetch —
the generated client is what the drift gate protects.
